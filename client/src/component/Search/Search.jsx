import React from "react";

function Search(){
    return(
        <div>
            <input>Buscar por Receta?</input>
            <button>Buscar</button>
        </div>
    )
}

export default (Search);